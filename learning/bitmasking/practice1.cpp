#include<stdio.h>
#include<string.h>
#include<string>

using namespace std;

int main() {
    int n = 5;
    int results[(1<<n)];
    memset(results, 0, sizeof(results));

    for(int i=0; i<(1<<n); ++i) {
       for(int j=0; j<n; j++) {
            
       } 
    }
    return 0;
}
